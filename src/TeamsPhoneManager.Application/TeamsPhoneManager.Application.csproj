<Project Sdk="Microsoft.NET.Sdk">

  <!--
    TeamsPhoneManager.Application — use cases and ports (Clean Architecture).

    Depends ONLY on Domain. MUST stay free of UI / IO frameworks: no Avalonia, no
    CommunityToolkit.Mvvm, no System.Management.Automation, no Microsoft.Identity.Client.
    (BCL types such as System.ComponentModel.INotifyPropertyChanged are allowed.)
    Enforced by DependencyRuleTests.
  -->
  <PropertyGroup>
    <RootNamespace>teams_phonemanager</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\TeamsPhoneManager.Domain\TeamsPhoneManager.Domain.csproj" />
  </ItemGroup>

</Project>
