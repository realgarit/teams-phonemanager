<UserControl x:Class="teams_phonemanager.Views.VariablesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:teams_phonemanager.Views"
             xmlns:viewmodels="clr-namespace:teams_phonemanager.ViewModels"
             xmlns:converters="clr-namespace:teams_phonemanager.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Welcome Message -->
        <TextBlock Text="{Binding WelcomeMessage}"
                 Style="{StaticResource MaterialDesignBody1TextBlock}"
                 TextWrapping="Wrap"
                 Margin="0,0,0,16"/>

        <!-- Variables Form -->
        <ScrollViewer Grid.Row="1" IsEnabled="{Binding CanProceed}">
            <StackPanel>
                <!-- General Variables -->
                <TextBlock Text="General Variables"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Customer Legal Name"
                             Text="{Binding Variables.CustomerLegalName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1"
                             materialDesign:HintAssist.Hint="MS Fallback Domain"
                             Text="{Binding Variables.MsFallbackDomain, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="1" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Customer"
                             Text="{Binding Variables.Customer, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="1" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Customer Group Name"
                             Text="{Binding Variables.CustomerGroupName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="2" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Language ID"
                             Text="{Binding Variables.LanguageId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="2" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Time Zone ID"
                             Text="{Binding Variables.TimeZoneId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="3" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Usage Location"
                             Text="{Binding Variables.UsageLocation, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="3" Grid.Column="1"
                             materialDesign:HintAssist.Hint="SKU ID"
                             Text="{Binding Variables.SkuId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="8,0,0,8"/>
                </Grid>

                <!-- Call Queues Variables -->
                <TextBlock Text="Call Queues Variables"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             materialDesign:HintAssist.Hint="CS App CQ ID"
                             Text="{Binding Variables.CsAppCqId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,0,8"/>
                </Grid>

                <!-- Auto Attendants Variables -->
                <TextBlock Text="Auto Attendants Variables"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0"
                             materialDesign:HintAssist.Hint="CS App AA ID"
                             Text="{Binding Variables.CsAppAaId, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Phone Number Type"
                             Text="{Binding Variables.PhoneNumberType, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="8,0,0,8"/>

                    <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="RAAA Number Name"
                             Text="{Binding Variables.RaaAnrName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,8"/>

                    <TextBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="RAAA Number"
                             Text="{Binding Variables.RaaAnr, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,8"/>

                    <TextBox Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="Default Call Flow Greeting Prompt (DE)"
                             Text="{Binding Variables.DefaultCallFlowGreetingPromptDE, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,8"/>

                    <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="After Hours Call Flow Greeting Prompt (DE)"
                             Text="{Binding Variables.AfterHoursCallFlowGreetingPromptDE, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2"
                             Text="Opening Hours 1"
                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                             Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBox Width="100"
                                 materialDesign:HintAssist.Hint="Start Time"
                                 Text="{Binding Variables.OpeningHours1Start, StringFormat=HH:mm}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,8,0"/>
                        <TextBox Width="100"
                                 materialDesign:HintAssist.Hint="End Time"
                                 Text="{Binding Variables.OpeningHours1End, StringFormat=HH:mm}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>

                    <TextBlock Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"
                             Text="Opening Hours 2"
                             Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                             Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBox Width="100"
                                 materialDesign:HintAssist.Hint="Start Time"
                                 Text="{Binding Variables.OpeningHours2Start, StringFormat=HH:mm}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Margin="0,0,8,0"/>
                        <TextBox Width="100"
                                 materialDesign:HintAssist.Hint="End Time"
                                 Text="{Binding Variables.OpeningHours2End, StringFormat=HH:mm}"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    </StackPanel>
                </Grid>

                <!-- Holidays Variables -->
                <TextBlock Text="Holidays Variables"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Holiday Name"
                             Text="{Binding Variables.HolidayName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,8,8"/>
                    <DatePicker Grid.Row="0" Grid.Column="1"
                               materialDesign:HintAssist.Hint="Holiday Date"
                               SelectedDate="{Binding Variables.HolidayDate, UpdateSourceTrigger=PropertyChanged}"
                               Style="{StaticResource MaterialDesignOutlinedDatePicker}"
                               Margin="8,0,0,8"/>

                    <TextBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="Holiday Greeting Prompt (DE)"
                             Text="{Binding Variables.HolidayGreetingPromptDE, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,0,8"/>
                </Grid>

                <!-- Auto-generated Variables -->
                <TextBlock Text="Auto-generated Variables"
                         Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                         Margin="0,0,0,16"/>
                
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Row="0" Grid.Column="0"
                             materialDesign:HintAssist.Hint="M365 Group"
                             Text="{Binding Variables.M365Group, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1"
                             materialDesign:HintAssist.Hint="RACQ UPN"
                             Text="{Binding Variables.RacqUPN, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="1" Grid.Column="0"
                             materialDesign:HintAssist.Hint="RACQ Display Name"
                             Text="{Binding Variables.RacqDisplayName, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="1" Grid.Column="1"
                             materialDesign:HintAssist.Hint="CQ Display Name"
                             Text="{Binding Variables.CqDisplayName, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="2" Grid.Column="0"
                             materialDesign:HintAssist.Hint="RAAA UPN"
                             Text="{Binding Variables.RaaaUPN, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,8,8"/>
                    <TextBox Grid.Row="2" Grid.Column="1"
                             materialDesign:HintAssist.Hint="RAAA Display Name"
                             Text="{Binding Variables.RaaaDisplayName, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="8,0,0,8"/>
                    
                    <TextBox Grid.Row="3" Grid.Column="0"
                             materialDesign:HintAssist.Hint="AA Display Name"
                             Text="{Binding Variables.AaDisplayName, Mode=OneWay}"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             IsReadOnly="True"
                             Margin="0,0,8,8"/>
                </Grid>

                <!-- Action Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Command="{Binding LoadVariablesCommand}"
                            Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FolderOpen" Margin="0,0,8,0"/>
                            <TextBlock Text="Load"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Command="{Binding SaveVariablesCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" Margin="0,0,8,0"/>
                            <TextBlock Text="Save"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl> 