# Store download hosting

Direct-download hosting for Microsoft Store package URLs (the Store rejects the 302 redirects of GitHub release asset URLs).
Do not modify or delete a version directory that is referenced by a live Store submission.
